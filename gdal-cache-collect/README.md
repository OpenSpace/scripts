A simple script that will go through all files in the `cache_gdal` folder and copy them to a `combined` folder and add a PNG file extension to them.

Copy this script to the `cache_gdal` folder and execute it.
